class_name: UIGuy
modules:
    enabled: [WebDriver, helpers\UIHelper]
    config:
        WebDriver:
            browser: firefox
            restart: false
            clear_cookies: true
            window_size: 1280x800
env:
    chrome:
         modules:
            config:
                WebDriver:
                    browser: chrome
