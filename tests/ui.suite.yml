class_name: UIGuy
modules:
    enabled: [WebDriver, helpers\UIHelper]
    config:
        WebDriver:
            browser: chrome
            restart: false
            clear_cookies: true
            window_size: 1280x800
env:
    firefox:
         modules:
            config:
                WebDriver:
                    browser: firefox
